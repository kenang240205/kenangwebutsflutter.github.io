name: super_app_kenang_web
description: Super App Kenang - Web friendly Flutter UI (3 pages, splash, animations)
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png